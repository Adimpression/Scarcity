---
swagger: "2.0"
info:
  description: "Scarcity Project Services"
  version: "1.1.0"
  title: "Scarcity"
host: "bkwsf9jwi1.execute-api.us-east-1.amazonaws.com"
basePath: "/development"
schemes:
- "https"
produces:
- "application/json"
paths:
  /notes:
    post:
      operationId: "notesPOST"
      parameters:
      - in: "body"
        name: "body"
        required: true
        schema:
          $ref: "#/definitions/Note"
      responses:
        200:
          description: "ok"
          schema:
            $ref: "#/definitions/Empty"
      x-amazon-apigateway-integration:
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        uri: "arn:aws:apigateway:us-east-1:lambda:path/2015-03-31/functions/arn:aws:lambda:us-east-1:990005713460:function:notesPOST/invocations"
        responses:
          default:
            statusCode: "200"
        type: "aws"
        parameters:
          body: "$input.body"
        requestTemplates:
          application/json: "{\n  \"body\" : \"$input.body\"\n}"
definitions:
  Note:
    type: "object"
    properties:
      name:
        type: "string"
        description: "Uninque Identifier for this note"
      value:
        type: "string"
        description: "Any text entry"
  Empty:
    type: "object"
